---
title: "UDE Quarto Slide"
subtitle: "A quick tour of Quarto"
author: "Jens Klenke"
date: today
bibliography: assets/references.bib         
format:
  revealjs:
    min-scale: 0.5      
    slide-number: true
    toc: true                         # show a side table-of-contents
    logo: assets/signet_ude_rgb.png      # appears bottom-right
    header-logo: "assets/LogoUniDuE.jpg"   # header logo
    incremental: False                 # bullets appear one-by-one
    # theme: [default, assets/clean.scss]
    smaller: true
    title-slide-attributes:
      data-background-image: assets/duetower.png
    html-math-method: mathjax
    include-in-header:
      - text: |
          <script>
          window.MathJax = {
            tex: {
              tags: 'ams'
            }
          };
          </script>  
execute:
  echo: true   
filters:
  - reveal-header     
---




## Introduction
::: {.column width="30%"}
![](assets/quarto_image.png)
:::
**Welcome**

- **Quarto** uses the same Markdown you already know  
- R/Python/Julia chunks run *live* during render  
- Export to HTML, PDF, PowerPoint from one source  

::: notes
Press **S** while presenting to open speaker notes.  
These notes are written in a fenced block starting with `::: notes`.
:::

---

**Bullets**

- Item A  
- Item B  
- Item C  

We can also set `incremental: true` in the YAML, each list item appears on click.


## Columns and call-outs

::: columns
::: {.column width="50%"}
::: {.callout-tip icon="lightbulb"}
### Tip
Quarto **call-outs** are the easiest way to highlight ideas.
:::
:::
::: {.column width="50%"}
::: {.callout-warning}
### Caution
2nd Example
:::
:::
:::

## Mathematics and reveal.js

- Consider, by way of illustration, the model
  
  \begin{align}
  \label{eq:regression}
  \text{E}(Y|X) & = \beta_0 + \beta_1 X_1 + \beta_2 X_2 + \ldots + \beta_k X_k \\
  \label{eq:variance}
  \text{Var}(Y|X) & = \sigma(X)^2
  \end{align}
  
- Note that \eqref{eq:regression} is a regression model and \eqref{eq:variance} is a volatility model

- Note also that we are using standard $\LaTeX$ syntax (e.g., `\text{E}(Y|X) & = \beta_0`) to render the mathematics

- As well, standard equation numbering and cross referencing via the `\label{}` and `\eqref{}` commands are supported thanks to MathJax

- Also, multi line equations are supported via `\begin{align}...\end{align}` etc.

## Static plot 

```{r}
library(plotly)
plot_ly(mtcars,
        x     = ~mpg,
        y     = ~hp,
        color = ~factor(cyl),
        type  = "scatter",
        mode  = "markers",
        width = 300,    
        height = 300 
        )

```

## Interactive Graph 

```{r}
library(leaflet)

leaflet(width  = "40%",    # 60% of slide width
        height = "200px") |>
  addTiles() |>
  setView(lng = 7.0084, lat = 51.4640, zoom = 16) |>
  addPopups(lng = 7.0084, lat = 51.4640,
            popup = "Chair of Econometrics")
```

## Interactive graph with sliding bar 

```{r}
ojs_define(actors = data.frame(
  x = rnorm(100),
  y = rnorm(100)
))
```

```{ojs}
//| panel: sidebar
viewof talentWeight = Inputs.range([-2, 2], { value: 0.7, step: 0.01, label: "talent weight" })
viewof looksWeight = Inputs.range([-2, 2], { value: 0.7, step: 0.01, label: "looks weight" })
viewof minimum = Inputs.range([-2, 2], { value: 1, step: 0.01, label: "min fame" })
```

```{ojs}
//| panel: fill
import { plotActors } from './actors.js';
plotActors(actors, talentWeight, looksWeight, minimum)
```

## Table  

| Default | Left | Right | Center |
|---------|:-----|------:|:------:|
| 12      | 12   |    12 |   12   |
| 123     | 123  |   123 |  123   |
| 1       | 1    |     1 |   1    |

: Demonstration of pipe table syntax

## Lets you run Python 


```{r}
library(reticulate)
# helper R code
x <- rnorm(1e5)
```


```{python}

import numpy as np
r_x = np.array(r.x)     # r.x is the R vector
r_x.mean()

```

Here’s a BibTeX entry you can drop into your references.bib to cite reference, in the same style [@Python3.11]

## References

::: {#refs}
:::